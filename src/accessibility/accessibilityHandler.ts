import { AccessibilityHandler, AccessibilityReport, AccessibilityWarning, AccessibilityError } from '../services/AccessibilityHandler';

/**
 * AccessibilityHandler provides methods to validate and audit AI-generated markdown for accessibility issues.
 * Ensures output meets WCAG 2.1 AA standards for color contrast, code blocks, heading structure, and links.
 */
export { AccessibilityHandler, AccessibilityReport, AccessibilityWarning, AccessibilityError };
